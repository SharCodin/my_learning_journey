int valueA = 1;
Console.WriteLine(valueA++);
// Output 1 -> Because valueA is retrieve first, then is printed in console and then ++ happens.

int valueB = 1;
Console.WriteLine(++valueB);
// Output 2 -> Because increment happens first, then valueB is printed in console.